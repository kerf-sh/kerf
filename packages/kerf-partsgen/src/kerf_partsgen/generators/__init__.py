"""Authored parametric generators — one module per part family.

Each ``<family_id>.py`` here is original MIT code authored once (by the
``author`` phase or by a human) and committed.  It freezes the family's
authoritative standard dimension table (``SIZES``) and builds geometry by
composing :mod:`kerf_partsgen.kernel`.  Standard *dimensions* are
uncopyrightable facts; the *code* is original.  See ``LICENSES.md``.
"""
